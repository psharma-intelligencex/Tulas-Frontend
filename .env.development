# Loaded automatically by `npm run dev` (Vite development mode); takes
# precedence over .env. Points the local frontend at the local backend so the
# whole stack (Vite + Express + PostgreSQL) runs on this machine.
VITE_SERVER_URL=http://localhost:3000
